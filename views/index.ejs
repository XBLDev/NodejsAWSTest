<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- Bootstrap core CSS -->
    <link href="<%= static_path + "/bootstrap/css/theme/" + theme + "/bootstrap.css" %>" rel="stylesheet">
    <!-- Custom styles for this template -->
    <link href="<%= static_path + "/bootstrap/css/jumbotron-narrow.css" %>" rel="stylesheet">

    <title>video Editing Nodejs test</title>

  </head>

  <body>

    <div class="container">

     <div id="convertSuccess" class="alert alert-success" style="display:none">
        <p id="convertSuccessText">Video convert success.</p>
     </div>

     <div id="convertFailed" class="alert alert-success" style="display:none">
        <p id="convertFailedText">Video convert failed.</p>
     </div>     
     
     <div>
        <p>Press the button to convert video</p>
        <button id="convertvideo" type="button" class="btn btn-primary">convert</button>
     </div>
    
    </div>


    <script src="<%= static_path + "/jquery/jquery-1.11.3.min.js" %>"></script>
    <script src="<%= static_path + "/bootstrap/js/bootstrap.min.js" %>"></script>

    <script type="text/javascript">
    $(document).ready(function() {
      $("#convertvideo").click(function() {
        $.post( "/convertvideo",
                function(data) {
                  $("#convertSuccess").show();                    
                                      
                }
              )
              .error(function(xhr) {
                $("#convertFailed").show();                                      
              });
      })
    })    
    </script>


  </body>
</html>      